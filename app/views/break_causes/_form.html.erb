<%= form_with(model: break_cause, local: true) do |form| %>
  <% if break_cause.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(break_cause.errors.count, "error") %> prohibited this break_cause from being saved:</h2>

      <ul>
      <% break_cause.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :break_cause_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
