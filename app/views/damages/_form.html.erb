<%= form_with(model: damage, local: true) do |form| %>
  <% if damage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(damage.errors.count, "error") %> prohibited this damage from being saved:</h2>

      <ul>
      <% damage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :damage_type_id %>
    <%= form.text_field :damage_type_id, id: :damage_damage_type_id %>
  </div>

  <div class="field">
    <%= form.label :expense_id %>
    <%= form.text_field :expense_id, id: :damage_expense_id %>
  </div>

  <div class="field">
    <%= form.label :driver_id %>
    <%= form.text_field :driver_id, id: :damage_driver_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
