<h4><%= t :drivers %></h4>

<!-- # -->
<div class="actions row">

  <div class="col-md-4">
    <%= link_to new_driver_path, :class => 'btn btn-info btn-sm' do %>
        <%= fa_icon 'plus' %> <%= t :add_a_new_driver %>
    <% end %>
  </div>

  <div class="col-md-4 ml-auto">
    <input type="search"
           class="form-control"
           id="search-driver"
           placeholder="<%= t :your_text %>..." aria-label="<%= t :your_text %>...">
  </div>

</div>

<!-- # -->
<table id="driver-data" class="responsive-table">
  <thead>
  <tr>
    <th scope="col"><%= t :full_name %></th>
    <th scope="col"><%= t :birth_date %></th>
    <th scope="col"><%= t :starting_date %></th>
    <th scope="col"><%= t :mobile_phone %></th>
    <th scope="col"><%= t :driver_license_number %></th>
    <th scope="col" class="td-actions">Actions</th>
  </tr>
  </thead>

  <tbody>
  <% @drivers.each do |driver| %>
      <tr>
        <th scope="row"><%= driver.full_name %></th>
        <td data-title="<%= t :birth_date %>"><%= driver.birth_date %></td>
        <td data-title="<%= t :starting_date %>"><%= driver.start_date %></td>
        <td data-title="<%= t :mobile_phone %>"><%= driver.mobile_phone %></td>
        <td data-title="<%= t :driver_license_number %>"><%= driver.driving_license_number %></td>
        <td>
          <%= link_to driver, :class => 'btn btn-outline-dark btn-sm' do %>
              <%= fa_icon 'eye' %>
          <% end %>
          <%= link_to edit_driver_path(driver), :class => 'btn btn-outline-dark btn-sm' do %>
              <%= fa_icon 'pencil' %>
          <% end %>
          <%= link_to driver, method: :delete, data: {confirm: "#{t :are_you_sure}"}, class: 'btn btn-outline-dark btn-sm' do %>
              <%= fa_icon 'close' %>
          <% end %>
        </td>
      </tr>
  <% end %>
  </tbody>

</table>