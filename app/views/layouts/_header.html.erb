 <header class="fixed-top">
    <nav class="col-sm-9 ml-sm-auto col-md-10 navbar navbar-expand-md">

      <!-- Navigation -->
      <div class="collapse navbar-collapse">

        <!--  # Left -->
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <a href="#"><i class="fa fa-navicon fa-lg"></i></a>
          </li>
        </ul>

        <!-- # Right -->
        <ul class="user-dropdown">

          <!-- # -->
          <li><a href="#" class="fa fa-expand fa-lg"></a></li>

          <!-- # -->
          <li class="dropdown">
            <a data-toggle="dropdown"
               href="#userMenuDropDown"
               aria-haspopup="true"
               aria-expanded="false"
               aria-controls="userMenuDropDown">

              <span class="pull-right">
                <% if current_manager.picture? %>
                  <%= image_tag(current_manager.picture) %>
                    <% else %>
                  <%= image_tag('default-user.png') %>
                <% end %>
              </span>
              <%= current_manager.full_name %>
            </a>

            <!-- # Drop Down -->
            <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="userMenuDropDown">
              <li class="dropdown-item">
                <%= link_to edit_manager_registration_path(current_manager) do %>
                    <%= fa_icon 'user' %> Mon Profile
                <% end %>
              </li>
              <li class="dropdown-item">
                <%= link_to destroy_manager_session_path, method: :delete do %>
                    <%= fa_icon 'close' %> Déconnexion
                <% end %>
              </li>
            </ul>
          </li>
        </ul>

      </div>

    </nav>
 </header>
