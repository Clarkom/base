<%= form_for @owner do |form| %>

    <div class="form-group row">
      <%= form.label 'Nom Complet', class: 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= form.text_field :full_name, class: 'form-control' %>
        <% if !@owner.errors.messages[:'full_name'].empty?  %>
            <ul>
              <% @owner.errors.messages[:'full_name'].each do |message| %>
                  <li>
                    <small class="form-text text-muted text-error"><%= message %></small>
                  </li>
              <% end %>
            </ul>
        <% end %>
      </div>
    </div>

    <div class="form-group row">
      <%= form.label 'Telephone Fixe', class: 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= form.text_field :home_phone, class: 'form-control' %>
      </div>
    </div>

    <div class="form-group row">
      <%= form.label 'Telephone Mobile', class: 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= form.text_field :mobile_phone, class: 'form-control' %>
        <% if !@owner.errors.messages[:'mobile_phone'].empty?  %>
            <ul>
              <% @owner.errors.messages[:'mobile_phone'].each do |message| %>
                  <li>
                    <small class="form-text text-muted text-error"><%= message %></small>
                  </li>
              <% end %>
            </ul>
        <% end %>
      </div>
    </div>

    <div class="actions">
      <%= form.submit 'Save', :class => 'btn btn-info' %>
    </div>

<% end %>
