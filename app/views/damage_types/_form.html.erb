<%= form_with(model: damage_type, local: true) do |form| %>
  <% if damage_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(damage_type.errors.count, "error") %> prohibited this damage_type from being saved:</h2>

      <ul>
      <% damage_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :damage_type_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
